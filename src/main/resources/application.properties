# Application Name
spring.application.name=Portfolio

# For local IDE run (connects to Docker MySQL via service name)
spring.datasource.url=jdbc:mysql://localhost:3307/portfolio?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Idjdidfc8.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG


# Optional: Ensure Hibernate uses the correct MySQL dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.open-in-view=false