services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_ROOT_PASSWORD=Idjdidfc8.'
      - 'MYSQL_DATABASE=portfolio'
    ports:
      - '3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: